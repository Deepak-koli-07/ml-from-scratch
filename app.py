import streamlit as st
import pandas as pd

st.set_page_config(page_title = 'Hello just for practice', page_icon = "👋", layout = 'wide')
st.title("👋 Hello, just for practice")
st.write("If you can read this, Streamlit is working.")